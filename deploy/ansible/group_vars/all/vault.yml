# ============================================================
# Ansible Vault 配置问题 - 密码未加密
# ============================================================

# 这个文件应该使用 ansible-vault 加密
# ansible-vault encrypt group_vars/all/vault.yml
# 但实际是明文存储

# 数据库凭证
vault_db_host: db.internal.company.com
vault_db_port: 5432
vault_db_user: admin
vault_db_password: "Password123!"
vault_db_name: production

# API 密钥
vault_stripe_key: "sk_live_4eC39HqLyjWDarjtT1zdp7dc"
vault_sendgrid_key: "SG.abcd1234.efgh5678ijkl9012mnopqrstuvwx"
vault_slack_webhook: "https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXXXXXX"

# SSH 密钥
vault_ssh_private_key: |
  -----BEGIN RSA PRIVATE KEY-----
  FAKE_KEY_FOR_DEMO
  -----END RSA PRIVATE KEY-----

# JWT 密钥
vault_jwt_secret: "super_secret_jwt_key_12345"

# AWS 凭证
vault_aws_access_key: "AKIAIOSFODNN7EXAMPLE"
vault_aws_secret_key: "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"

# 问题总结:
# 1. 所有敏感信息都是明文存储
# 2. 应该使用 ansible-vault 加密
# 3. vault 密钥文件应该在 .gitignore 中
# 4. 不应该将 vault 文件提交到 Git
# 5. 应该使用外部密钥管理 (HashiCorp Vault, AWS Secrets Manager)

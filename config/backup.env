# ============================================================
# Backup 配置模板 - 实际值通过环境变量注入
# ============================================================

# 数据库连接（从环境变量读取）
OLD_DB_HOST=${OLD_DB_HOST}
OLD_DB_USER=${OLD_DB_USER}
OLD_DB_PASSWORD=${OLD_DB_PASSWORD}

# AWS 凭证（使用 IAM 角色，不使用硬编码密钥）
# AWS_ACCESS_KEY_ID 和 AWS_SECRET_ACCESS_KEY 通过 IAM 角色获取

# 服务令牌（从 Secret Manager 读取）
# KUBERNETES_TOKEN 通过 ServiceAccount 获取

# 第三方服务（从环境变量读取）
# DATADOG_API_KEY=${DATADOG_API_KEY}
# NEW_RELIC_KEY=${NEW_RELIC_KEY}
